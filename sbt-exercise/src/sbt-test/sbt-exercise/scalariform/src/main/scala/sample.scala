//

object PoorlyFormatted extends App {
  println("it's " + { 1  + {
  // this file
    // is poorly
      // formatted

      Option(1)
    .map(_ + 1)
  .map(_ * 10)
    .map(_ /
11) getOrElse   100
}

}        )        }
